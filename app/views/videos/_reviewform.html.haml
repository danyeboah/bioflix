= bootstrap_form_for([@video,@review]) do |f|
  .row
    .col-sm-3
      =f.select :rating, rating_options, {label: "Rate this video", include_blank: true}
  .row
    .col-sm-7
      = f.text_area :content, label: "Write a review"
      = f.submit "Submit", class: "btn btn-small btn-success"  